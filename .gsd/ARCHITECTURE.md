# Architecture

> Auto-generated by /map on 2026-02-11

## Overview

Us2gether is a relationship-focused mobile application built with React Native (Expo), using Convex for the backend and Clerk for authentication. It facilitates interaction between partners likely through shared data and real-time updates.

```
┌─────────────────────────────────────────┐
│              App (Expo Router)          │
│        [app/_layout.tsx Entry]          │
├──────────────────────┬──────────────────┤
│    Auth (Clerk)      │   UI Components  │
│ [providers/Clerk...] │  [components/*]  │
├──────────────────────┴──────────────────┤
│           Data Layer (Convex)           │
│         [convex/* functions]            │
└─────────────────────────────────────────┘
```

## Components

### Frontend Application

- **Purpose:** Main user interface and navigation logic.
- **Location:** `app/`
- **Dependencies:** `expo-router`, `react-native`, `nativewind`

### Authentication

- **Purpose:** User identity and session management.
- **Location:** `providers/ClerkProvider.tsx`, `app/(auth)/`
- **Dependencies:** `@clerk/clerk-expo`

### Backend / Data API

- **Purpose:** Serverless backend for data persistence and business logic.
- **Location:** `convex/`
- **Dependencies:** `convex`
- **Key Files:** `schema.ts`, `users.ts`, `invitations.ts`, `couples.ts`

### State Management

- **Purpose:** Client-side state.
- **Location:** `store/`
- **Dependencies:** `zustand`

### Storage

- **Purpose:** Local persistence (likely for preferences/cache).
- **Location:** `storage/`
- **Dependencies:** `react-native-mmkv`

## Data Flow

1. **User Action**: Component triggers an event (e.g., `useMutation` hook).
2. **API Call**: Convex client sends request to Convex backend.
3. **Authentication**: Request is authenticated via Clerk token.
4. **Business Logic**: Convex mutation/query executes (`convex/*.ts`).
5. **Database**: Data is read/written to Convex database.
6. **Update**: React components re-render with new data (reactive subscriptions).

## Integration Points

| Service | Type                 | Purpose                                |
| ------- | -------------------- | -------------------------------------- |
| Convex  | Backend as a Service | Database, Functions, Real-time updates |
| Clerk   | Auth Provider        | User management, Authentication        |
| Expo    | Framework            | Native runtime, Build system           |

## Technical Debt

- [ ] Nos specific TODO comments found in codebase search.
- [ ] Verify validation rules in `convex/schema.ts`.
- [ ] Ensure consistent error handling across all API calls.

## Conventions

**Naming:**

- Components: PascalCase (e.g., `PartnerCard.tsx`)
- Hooks: camelCase with `use` prefix (e.g., `useUser.ts`)
- Convex Functions: camelCase (e.g., `createUser`)

**Structure:**

- Expo Router file-based routing in `app/`.
- Feature-based grouping for components (`components/home`, `components/auth`).

**Testing:**

- No test files detected (e.g., `__tests__` directories).
