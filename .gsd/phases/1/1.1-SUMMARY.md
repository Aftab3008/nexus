## Plan 1.1 Summary

### Tasks Completed

- [x] **Update Schema**: Added `letters` table with indexes for efficient querying.
- [x] **Create Letters API**: Implemented `send`, `list`, `markRead`, and `getById` functions in `convex/letters.ts`.

### Verification

- Schema pushed to Convex via `npx convex dev --once`.
- Types generated successfully.
