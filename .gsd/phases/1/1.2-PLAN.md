---
phase: 1
plan: 1.2
wave: 2
---

# Plan 1.2: Rich Text Editor & Sending UI

## Objective

Build the frontend experience for writing and sending letters.

## Context

- .gsd/DECISIONS.md
- components/letters/LetterEditor.tsx
- app/(home)/write-letter.tsx

## Tasks

<task type="auto">
  <name>Create Letter Editor Component</name>
  <files>components/letters/LetterEditor.tsx</files>
  <context>
    - Use `ui-ux-pro-max` skill for premium design.
    - Use `expo-skills` for best practices.
  </context>
  <action>
    - Implement a rich text editor (e.g., using `react-native-cn-quill` or a simple customized TextInput with style toggles if quill is too heavy). *Decision: Start with simple styled TextInput + Image attachment for MVP to keep it lightweight.*
    - Props: `onSend(content, scheduledTime)`, `initialContent`.
    - Styling: Paper/Notebook aesthetic.
  </action>
</task>

<task type="auto">
  <name>Implement Write Letter Screen</name>
  <files>app/(home)/write-letter.tsx</files>
  <context>
    - Use `mcp_clerk` tools (`clerk_sdk_snippet`) to implement correct user authentication checks (`useUser`).
    - Use `mcp_convex` tools to verify mutation signatures.
    - Use `ui-ux-pro-max` for layout.
  </context>
  <action>
    - Use `LetterEditor` component.
    - Add "Instant" vs "Scheduled" toggle.
    - "Send" button triggers animation (see Plan 1.4).
    - Validates input before sending.
    - Calls `api.letters.send`.
  </action>
</task>

## Success Criteria

- [ ] User can type text and attach an image (optional)
- [ ] User can choose delivery time
- [ ] Sending calls the backend mutation
