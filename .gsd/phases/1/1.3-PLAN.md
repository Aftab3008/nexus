---
phase: 1
plan: 1.3
wave: 2
---

# Plan 1.3: Mailbox & Reading UI

## Objective

Implement the inbox for listing and reading letters.

## Context

- components/letters/LetterListItem.tsx
- app/(home)/letters.tsx
- app/(home)/letter/[id].tsx

## Tasks

<task type="auto">
  <name>Create Mailbox List</name>
  <files>app/(home)/letters.tsx, components/letters/LetterListItem.tsx</files>
  <context>
    - Use `mcp_clerk` tools to ensure user context is checked.
    - Use `mcp_convex` tools (`runOneoffQuery`) to verify data availability.
    - Use `ui-ux-pro-max` skill for design.
    - Use `code-simplifier` skill for clean code.
  </context>
  <action>
    - Fetch letters using `api.letters.list`.
    - `LetterListItem`: Show sender, preview (if read), date, and status (unread/locked).
    - Separation of "Inbox" and "Sent".
  </action>
</task>

<task type="auto">
  <name>Implement Letter Detail View</name>
  <files>app/(home)/letter/[id].tsx</files>
  <context>
    - Use `ui-ux-pro-max` skill for reading experience.
  </context>
  <action>
    - Fetch full letter details.
    - Mark as read on open (useEffect).
    - Render body content.
    - Back navigation.
  </action>
</task>

## Success Criteria

- [ ] User can see list of received letters
- [ ] Opening a letter marks it as read
- [ ] User can see sent letters history
